# CMS

Reserved workspace for Payload CMS backend concerns.

Current status: Payload still runs inside `apps/web` through Next.js routes:

- `/admin`
- `/api`
- GraphQL endpoint

Use this folder later when the CMS is split into its own runtime.
