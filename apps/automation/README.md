# Automation

Workspace for content operations and automation.

Planned responsibilities:

- Read Google Sheet rows marked `ready`
- Validate content fields, slugs, categories, and duplicates
- Create Payload drafts only
- Write `payload_id`, `preview_url`, `status`, and `error_message` back to the sheet
- Expose MCP/AI workflows for draft creation

Publishing must stay manual through Payload.
